@layout('layouts/app')
@section('content')
  <div class="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8">
    <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
      <h1 class="text-2xl font-bold text-gray-900 mb-4">Integration Components Test</h1>
      
      <div class="mb-8">
        <h2 class="text-lg font-semibold text-gray-900 mb-4">Platform Logos</h2>
        <div class="flex space-x-4">
          @include('components/platform-logo', { platform: 'google_ads', size: 'sm' })
          @include('components/platform-logo', { platform: 'google_ads', size: 'md' })
          @include('components/platform-logo', { platform: 'google_ads', size: 'lg' })
        </div>
        <div class="flex space-x-4 mt-4">
          @include('components/platform-logo', { platform: 'meta_ads', size: 'sm' })
          @include('components/platform-logo', { platform: 'meta_ads', size: 'md' })
          @include('components/platform-logo', { platform: 'meta_ads', size: 'lg' })
        </div>
        <div class="flex space-x-4 mt-4">
          @include('components/platform-logo', { platform: 'tiktok_ads', size: 'sm' })
          @include('components/platform-logo', { platform: 'tiktok_ads', size: 'md' })
          @include('components/platform-logo', { platform: 'tiktok_ads', size: 'lg' })
        </div>
      </div>
      
      <div class="mb-8">
        <h2 class="text-lg font-semibold text-gray-900 mb-4">Sync Status</h2>
        <div class="flex space-x-4">
          @include('components/sync-status', { status: 'active' })
          @include('components/sync-status', { status: 'syncing' })
          @include('components/sync-status', { status: 'error' })
          @include('components/sync-status', { status: 'inactive' })
        </div>
      </div>
      
      <div class="mb-8">
        <h2 class="text-lg font-semibold text-gray-900 mb-4">Integration Cards</h2>
        <div class="space-y-4">
          @include('components/integration-card', { 
            platform: { 
              name: 'google_ads', 
              displayName: 'Google Ads', 
              description: 'Connect your Google Ads account', 
              connected: false 
            } 
          })
          @include('components/integration-card', { 
            platform: { 
              name: 'meta_ads', 
              displayName: 'Meta Ads', 
              description: 'Connect Facebook & Instagram ads (Coming Soon)', 
              connected: false,
              disabled: true
            } 
          })
        </div>
      </div>
      
      <div>
        <h2 class="text-lg font-semibold text-gray-900 mb-4">Account Summary</h2>
        <div class="max-w-md">
          @include('components/account-summary', {
            account: {
              platform: 'google_ads',
              accountId: '1234567890',
              isActive: true
            }
          })
        </div>
      </div>
    </div>
  </div>
@endsection

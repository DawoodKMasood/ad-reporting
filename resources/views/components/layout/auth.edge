<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
      {{ title || 'Ad Reporting Tool' }}
    </title>
    @vite(['resources/js/app.js'])
    <link href="/css/fontawesome.css" rel="stylesheet" />
    <style>
      body {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      }
    </style>
    @if ($slots.meta)
      {{{ await $slots.meta() }}}
    @endif
  </head>
  <body class="min-h-screen font-sans">
    <div id="app">
      <!-- Flash Messages -->
      @if(flashMessages && flashMessages.has && flashMessages.has('success'))
        <div
          class="fixed top-4 left-1/2 transform -translate-x-1/2 bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded z-50"
        >
          <i class="fas fa-check-circle mr-2"></i>
          {{ flashMessages.get('success') }}
        </div>
      @end
      
      @if(flashMessages && flashMessages.has && flashMessages.has('error'))
        <div
          class="fixed top-4 left-1/2 transform -translate-x-1/2 bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded z-50"
        >
          <i class="fas fa-exclamation-circle mr-2"></i>
          {{ flashMessages.get('error') }}
        </div>
      @end
      
      <div class="min-h-screen flex items-center justify-center px-4 sm:px-6 lg:px-8">
        <div class="max-w-md w-full">
          {{{ await $slots.main() }}}
        </div>
      </div>
    </div>
  </body>
</html>
